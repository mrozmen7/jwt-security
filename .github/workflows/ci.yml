name: CI

on:
  pull_request:
  push:
    branches: [ "main" ]

jobs:
  required-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Required files must exist
        run: |
          test -f README.md
          test -f CLAUDE.md
